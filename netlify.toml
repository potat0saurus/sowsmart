[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  VITE_BASE = "/"

# SPA fallback — send all routes to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
